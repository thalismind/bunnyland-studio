{
  "name": "bunnyland-studio-repo",
  "private": true,
  "version": "0.1.0",
  "license": "AGPL-3.0-or-later",
  "workspaces": ["web"],
  "scripts": {
    "build": "npm --workspace web run build",
    "test": "npm --workspace web test",
    "check": "npm --workspace web run check"
  }
}
